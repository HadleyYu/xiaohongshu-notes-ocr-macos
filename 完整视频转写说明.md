# 完整视频转文字

双击桌面的「小红书文字提取.command」。

1. 选 `1`，或直接按回车，进入完整视频模式。
2. 粘贴小红书分享链接（整段分享文案也可以）。
3. 按提示打开专用浏览器；由你手动登录。回到终端继续。
4. 目标笔记打开后，确认视频能播放，必要时点击播放，然后回终端按回车。
5. 等待下载和本地语音识别。看到「视频语音转写完成」后，打开生成的 `标题_视频全文.md`。

输出与之前的 Markdown 在同一个 `xiaohongshu-notes-ocr-macos` 目录，不放进「文稿」或程序子目录。

## 更直接的方法：拖入已有视频

选 `1` 后，不粘贴链接，直接从 Finder 把完整视频或音频文件拖进终端，再按回车。不需要登录浏览器。

支持 MP4、MOV、MKV、WEBM、M4V、MP3、M4A、WAV、AAC、FLAC、AIFF。

## 结果是什么

- 处理整段文件的声音，不再只识别封面。包含时间戳和按顺序的讲话文字，不自动改写成摘要。
- 发布者的配文另列，明确标注，不冒充视频讲话。
- 自动语音识别可能错字、漏词，科研术语和人名请核对。当前以中文讲话为主。
- 不提取「只显示在课件、板书上而没有念出来」的文字；该功能需要另外做画面 OCR。
- 检查文件有音轨、文件时长，并在取得网页时长时进行比对。但成功转写并不保证每个字都正确。

## 模型、隐私与故障

- 使用 [MLX Whisper](https://github.com/ml-explore/mlx-examples/tree/main/whisper) 在本机识别。
- 首次下载 [large-v3-turbo 4-bit 压缩模型](https://huggingface.co/mlx-community/whisper-large-v3-turbo-q4)，约 464 MB，放在工具的 `.models` 内；之后离线复用。模型下载需联网，视频/音频不上传到语音服务。下载过程中会显示进度。
- 仅复用本工具 `.login-browser` 中的登录，不读取日常 Chrome 登录资料，不绕过登录、验证码或视频访问限制。
- 网页改版、视频流形式或权限可能导致链接提取失败。此时工具会明确停止，不会退回封面 OCR 假装完成；可用你已保存的完整视频走本地模式。
- 失败时下载文件保留在 `.video-work/run-…/video.mp4`，可将有效文件拖入重试；成功后自动清理本次下载的临时视频，原始本地文件从不删除。
- 不在 Markdown 保存分享认证参数。不要分享 `.login-browser`，其中有登录信息。

## 图文笔记

启动时选 `2`，沿用正文＋图片 OCR。视频请选 `1`。

## 目录兼容

支持程序文件在主目录，或目前的「未命名文件夹」中。Python 环境、浏览器、登录状态和模型仍在主目录。请不要再单独移动这些隐藏文件夹。
