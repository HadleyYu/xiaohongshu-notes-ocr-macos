# 分享链接识别修复｜2026-09-18

已修复原版本只支持 `xhslink.com`、不识别 `xhslink.cn` 的问题；支持直接链接、带文字的分享内容、Markdown 方括号链接和尖括号链接。重定向后的访问参数保留，不放宽到任意外部域名。

你的测试链接 `https://xhslink.cn/o/2jlnsN1y3LH` 已通过公开 HTTP 实际请求：返回 HTTP 200，但终点是小红书 `/login` 页面，不是笔记正文。因此需要登录才能继续，未取得正文；本次没有连接本机 Chrome、读取 Cookie 或登录账号。

现在会明确提示“要求登录后查看”，不再误报链接格式不支持。后续已补充[专用浏览器登录流程](登录后提取说明.md)：你确认并手动登录后，短链解析与内容提取使用同一会话。仅在日常浏览器登录不会自动生效，不能把登录门槛页保存成正文。

回归：新增 13 项分享格式/错误提示测试，原有 14 项 URL 解析测试，共 27 项通过。网络与浏览器在回归中均作模拟；实际短链请求另行核对。原文件保留为 `xhs_url_validator.before-cn-fix.py`，未改变之前记录的上游其他 8 项失败。

“直播切片”若为视频笔记，本工具只能提取标题、配文；不能把视频讲话直接转成全文。口述内容需要独立的语音转写步骤。
